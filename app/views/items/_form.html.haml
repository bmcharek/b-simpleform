= simple_form_for(@item) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :description, label: "afb"
    = f.file_field :avatar

  .form-actions
    = f.button :submit
